services:
  - type: web
    name: event-management-api
    env: node
    region: oregon
    plan: free
    buildCommand: cd backend && npm install --include=dev && npm run build && npm run db:push
    startCommand: cd backend && npm run start:prod
    envVars:
      - key: NODE_ENV
        value: production

